import { Grid } from '@mui/material';
import React from 'react';

const Header: React.FC = () => {
  return (
    <Grid sx={{ height: 50, width: '100%', backgroundColor: 'black' }}></Grid>
  );
}

export default Header;